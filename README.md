Running `./run.sh init` will initialize and pair both nodes. With two different
terminals, running `./run.sh node01`, and `./run.sh node02`, respectively, we
can see blocks being produced and finalized.
